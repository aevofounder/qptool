# Base URL for the Django API. Leave as the relative default to use the Vite
# dev proxy (recommended in development). In production set the full origin,
# e.g. https://api.qptool.ru/api
VITE_API_BASE=/api
